<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zero.sys.mapper.UserMapper">

    <resultMap id="resultMap" type="com.zero.sys.domain.User">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="username" jdbcType="VARCHAR" property="username"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="enabled" jdbcType="BOOLEAN" property="enabled"/>
        <result column="locked" jdbcType="BOOLEAN" property="locked"/>
        <result column="account_expire" jdbcType="BOOLEAN" property="accountExpire"/>
        <result column="password_expire" jdbcType="BOOLEAN" property="passwordExpire"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <collection property="roles" ofType="com.zero.sys.domain.Role">
            <id column="rid" jdbcType="INTEGER" property="id"/>
            <result column="name" jdbcType="VARCHAR" property="name"/>
            <result column="description" jdbcType="VARCHAR" property="description"/>
        </collection>
    </resultMap>

    <sql id="baseColumn">
        id, username, `password`, enabled, locked, account_expire, password_expire, create_time, update_time
    </sql>

    <sql id="baseSelectSql">
        select su.id as id,su.username as username,su.password as password,
        su.enabled as enabled,su.locked as locked,su.account_expire as account_expire,
        su.password_expire as password_expire,sr.id as rid,sr.name as name,
        sr.description as description
        from sys_user su
        left join sys_user_role sur on sur.user_id = su.id
        left join sys_role sr on sr.id = sur.role_id
    </sql>


    <select id="loadUserByUsername" resultMap="resultMap">
        <include refid="baseSelectSql"></include>
        where su.username = #{username}
    </select>

    <select id="getPage" resultMap="resultMap">
        <include refid="baseSelectSql"></include>
    </select>

    <delete id="deleteUserRole">
        delete from sys_user_role where user_id = #{userId} and role_id = #{roleId}
    </delete>



</mapper>